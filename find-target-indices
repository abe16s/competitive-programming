class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums.sort()
        cout=[]
        for i in range(len(nums)):
            if nums[i]==target:
                cout.append(i)
        return cout
